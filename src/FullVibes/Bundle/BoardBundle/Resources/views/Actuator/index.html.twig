{% extends 'layout.html.twig' %}

{% block title %}RaspiPlant | Actuator{% endblock %}

{% block page_title %}
    RaspiPlant Actuators
    <small>board actuators </small>
{% endblock %}

{% block page_breadcrumb %}                                
    <li>
        <a href="{{path('board.dashboard.main')}}">Home</a>
        <i class="fa fa-circle"></i>
    </li>
    <li>
        <a href="{{path('board.actuator.main')}}">Actuator</a>
        <i class="fa fa-circle"></i>
    </li>
{% endblock %}

{% block page_content %}
    {% for flash_message in app.session.flashBag.get('notice') %}
        <div class="flash-notice">
            {{ flash_message }}
        </div>
    {% endfor %}

    <!-- END PAGE TITLE-->
    <!-- END PAGE HEADER-->
    <!-- BEGIN DASHBOARD STATS 1-->
    <div class="row">
        <div class="table-responsive">
            <table class="table table table-condensed table-hover">
                <thead>
                    <tr>
                        <th>#</th> <th>Name</th> <th>Slug</th> <th>Class</th> <th>pin</th> <th>state</th>
                    </tr>
                </thead>
                <tbody>
                    {% for actuator in actuators %}
                    <tr>
                        <td>{{ actuator.id }}</td><td>{{ actuator.name }}</td><td>{{ actuator.slug }}</td><td>{{ actuator.class }}</td><td>{{ actuator.pin }}</td><td>{{ actuator.state }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
{% endblock %}
