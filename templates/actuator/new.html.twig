{% extends 'layout.html.twig' %}

{% form_theme form 'form/simple.form.twig' %}

{% block page_content %}
    <h1>Actuator creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a class="btn btn-primary" href="{{ path('actuator_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
